// Package tui renders the lazygh terminal user interface. It should depend on
// capability ports and provider-neutral GitHub domain values at the boundary,
// leaving transport-specific `gh` details in `internal/githubcli`.
package tui
