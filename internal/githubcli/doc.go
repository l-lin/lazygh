// Package githubcli adapts the `gh` CLI behind focused services and domain-facing
// capability adapters. Keep shell transport, command formatting, and GitHub API
// quirks here; product-facing models live in `internal/github`.
package githubcli
